-- =========================================================
-- 직원 구매 추가: 계좌이체·카드 결제 증빙 사진 (이체 완료 화면 / 카드 승인 문자 캡처)
-- "이체했다", "카드로 냈다"고 하고 실제로는 안 낸 경우를 막기 위한 것.
-- 사용법: Supabase 대시보드 -> SQL Editor -> New query -> 전체 붙여넣기 -> Run (한 번만, 여러 번 실행해도 안전)
-- =========================================================
alter table ops_staff_purchases add column if not exists pay_proof_path text;   -- 계좌이체 완료 화면·카드 승인 문자 캡처 사진 경로 (receipts 버킷)
