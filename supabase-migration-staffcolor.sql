-- =========================================================
-- 근무표: 직원마다 색을 고정으로 저장 (매주 자동 배정 대신 항상 같은 색)
-- 사용법: Supabase 대시보드 -> SQL Editor -> New query -> 전체 붙여넣기 -> Run (한 번만)
-- =========================================================

alter table manual_staff add column if not exists color text;
