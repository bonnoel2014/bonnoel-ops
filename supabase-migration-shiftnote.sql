-- =========================================================
-- 근무표: 근무 한 건마다 짧은 메모(멘트) 남기기
-- 사용법: Supabase 대시보드 -> SQL Editor -> New query -> 전체 붙여넣기 -> Run (한 번만)
-- =========================================================

-- 예: "실제 출근은 7:20", "휴게 30분→40분으로 변경" 같은 짧은 설명을 근무 한 줄에 붙여둘 수 있음
alter table ops_shift_patterns add column if not exists note text;
